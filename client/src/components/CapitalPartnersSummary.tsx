/**
 * Deliberately hidden until Capital moves from the unscoped legacy ledger to
 * company-scoped saas_* records. The API is also fail-closed on the server.
 */
export function CapitalPartnersSummary(_props: { ownerId?: number }) {
  return null;
}
